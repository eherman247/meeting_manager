export const getTimeOffsDay = (timeOffs, day) => {
  const dayTimeOffs = new Map()

  // console.log(timeOffs)

  // {timeOffs && timeOffs.map((timeOff) => {
  //   if(timeOff.day === day){
  //     dayTimeOffs.set(timeOff)
  //   }
  // }
  //       )}

  return(
    dayTimeOffs
  )
}