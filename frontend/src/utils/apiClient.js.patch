*** Begin Patch
*** Update File: c:\Users\eherm\Documents\meeting_manager\frontend\src\utils\apiClient.js
@@
-  const apiRoot =
-    process.env.REACT_APP_API_BASE_URL ||
-    (process.env.NODE_ENV === "development" ? "http://localhost:4000" : "");
+  const localDevRoot =
+    typeof window !== "undefined" &&
+    (window.location.hostname === "localhost" ||
+      window.location.hostname === "127.0.0.1")
+      ? "http://localhost:4000"
+      : "";
+  const apiRoot =
+    process.env.REACT_APP_API_BASE_URL ||
+    localDevRoot ||
+    (process.env.NODE_ENV === "development" ? "http://localhost:4000" : "");
*** End Patch